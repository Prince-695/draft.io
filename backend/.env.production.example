# ============================================================
# Draft.IO — Production Environment Variables
# Copy this to .env before running docker compose locally,
# or paste these into Railway's "Variables" tab per service.
# ============================================================

# ── Secrets (CHANGE ALL OF THESE) ───────────────────────────

# Generate with:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_ME_random_32_byte_hex
JWT_REFRESH_SECRET=CHANGE_ME_different_random_32_byte_hex

# ── Your deployed frontend URL ───────────────────────────────
# Set this to your Vercel URL, e.g. https://draft-io.vercel.app
FRONTEND_URL=https://your-frontend.vercel.app

# ── OpenAI ───────────────────────────────────────────────────
OPENAI_API_KEY=sk-proj-...your-key-here...

# ── Cloudinary (for image uploads) ──────────────────────────
# Sign up free at https://cloudinary.com
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ── Google OAuth (optional) ──────────────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=https://your-railway-domain/auth/google/callback

# ── Database credentials (only needed for local docker compose)
# When deploying to Railway, Railway injects these automatically.
# You do NOT need to change these for Railway.
POSTGRES_USER=draftio
POSTGRES_PASSWORD=draftio123
POSTGRES_DB=draftio
MONGO_USER=draftio
MONGO_PASSWORD=draftio123
